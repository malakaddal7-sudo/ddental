name: dental_clinic
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  url_launcher: ^6.1.11
  http: ^1.2.0
  file_picker: ^6.1.1
  pdf: ^3.10.7
  printing: ^5.12.0
  # FIX: added image_picker — required by xray_upload_widget.dart
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true